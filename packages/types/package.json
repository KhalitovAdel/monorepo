{
    "name": "@monorepo/types",
    "version": "0.0.1",
    "license": "MIT",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -b tsconfig.build.json",
        "prebuild": "yarn clean",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "^4.5.2"
    }
}
